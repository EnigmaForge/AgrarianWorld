namespace Modules.GenerationSystem {
    public enum LSystemCommand {
        Unknown = '1',
        Save = '[',
        Load = ']',
        Draw = 'F',
        TurnRight = '+',
        TurnLeft = '-'
    }
}